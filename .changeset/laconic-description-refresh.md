---
"@zcaceres/skill-laconic": patch
---

Docs: refresh the `package.json` description. It predated 0.5.0/0.6.0 and
omitted `laconic-code` mode and the configurable cadence, so npm and package
listings showed a stale summary. Now matches the SKILL.md frontmatter.
