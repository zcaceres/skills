---
"@zcaceres/skill-pr": patch
---

pr: move the six git workflow references (`update`, `checkpoint`, `submit`, `sync`, `merge`, `log`) into `references/git/` to make room for a parallel `jj` path. Pure relocation — link paths updated, no behavior change.
