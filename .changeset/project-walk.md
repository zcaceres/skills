---
"@zcaceres/skill-project": minor
---

Add `/project walk` for card-by-card grooming of a milestone, status/column, label, or default not-Done scope. Each card shows an evidence-backed status-or-keep suggestion; Enter accepts it, while status, update, keep, details, delete, and quit remain available. Actions apply as the walk advances and retain the existing update and delete safety gates.

Rename `/project review` to `/project audit` so board auditing is not confused with source-code review. The old project subcommand is removed rather than retained as an alias.
