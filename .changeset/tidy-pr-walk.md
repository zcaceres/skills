---
"@zcaceres/skill-pr": minor
---

Add `/pr walk` to atomically generate exact patch and Markdown review packets with one notes area per open PR, render complete PRs in chat with structured navigation and explanation controls, reject cross-repository selectors, exclude unrelated fork PRs with colliding branch names from stack discovery, detect state/head/base and discussion drift during review, and safely apply, restack, push, and sync approved changes as one stack-wide operation. Binary capability checks also replace stale pre-walk helper binaries during upgrades.
