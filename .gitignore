node_modules/
dist/
.DS_Store
*.log

# Per-skill build output and fetched binaries (rebuilt/redownloaded on demand)
skills/*/dist/
skills/*/scripts/bin/
skills/*/*.tgz

# Claude Code worktrees (created by `claude --worktree`; set up by quality-worktree-setup)
.claude/worktrees/
