node_modules/
dist/
.DS_Store
*.log

# Python bytecode (skill helpers compile these on run)
__pycache__/
*.pyc

# Per-skill build output and fetched binaries (rebuilt/redownloaded on demand)
skills/*/dist/
skills/*/scripts/bin/
skills/*/*.tgz

# Claude Code worktrees (created by `claude --worktree`; set up by quality-worktree-setup)
.claude/worktrees/

# Personal skill-install artifacts (from `npx skills add`) and settings backups
skills-lock.json
*.bak.*
