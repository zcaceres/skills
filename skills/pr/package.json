{
  "name": "@zcaceres/skill-pr",
  "version": "2.2.0",
  "description": "Commit your work and ship it as stacked PRs with /pr or $pr in Codex. Bare invocation checkpoints the current diff as the next branch in a stack; subcommands publish the stack (submit), review it as annotatable Markdown with colorized git-delta diffs and exact patches (walk), rebase it onto trunk (sync), and land it bottom-up (merge). Update covers the single-branch case. Open any PR as a draft with --draft, or make drafts the default via setup. Includes an opt-in diff-size nudge hook for Claude Code, Codex, and Gemini CLI. Uses git stack when installed, falls back to gh + git. Optional Jujutsu (jj) backend for colocated repos.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/index.ts",
    "test": "bun test",
    "build:linux": "bun build scripts/index.ts --compile --target=bun-linux-x64 --outfile scripts/bin/pr-nudge-linux-x64",
    "build:macos": "bun build scripts/index.ts --compile --target=bun-darwin-arm64 --outfile scripts/bin/pr-nudge-darwin-arm64",
    "build:windows": "bun build scripts/index.ts --compile --target=bun-windows-x64 --outfile scripts/bin/pr-nudge-windows-x64.exe",
    "build:all": "bun run build:linux && bun run build:macos && bun run build:windows",
    "build": "bun run build:all",
    "fetch-tools": "bun run build:all"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "files": [
    "SKILL.md",
    "scripts",
    "references",
    "assets",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "claude-code",
    "codex",
    "skill",
    "slash-command",
    "hook",
    "pull-request",
    "stacked-prs",
    "git",
    "github",
    "jujutsu",
    "jj"
  ]
}
