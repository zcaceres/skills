{
  "name": "@zcaceres/skill-safety-op-creds",
  "version": "0.0.0",
  "description": "Claude Code skill: fetch credentials from 1Password via the `op` CLI and feed them to programs through process-substitution file descriptors or `op run` env vars, so secrets never touch disk or the agent's transcript. Ships a `with-creds` wrapper and a PreToolUse hook that blocks bare `op read` and other secret-printing op subcommands.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/index.ts",
    "test": "bun test",
    "build:linux-x64": "bun build scripts/index.ts --compile --target=bun-linux-x64 --outfile scripts/bin/safety-op-creds-linux-x64",
    "build:linux-arm64": "bun build scripts/index.ts --compile --target=bun-linux-arm64 --outfile scripts/bin/safety-op-creds-linux-arm64",
    "build:darwin-arm64": "bun build scripts/index.ts --compile --target=bun-darwin-arm64 --outfile scripts/bin/safety-op-creds-darwin-arm64",
    "build:darwin-x64": "bun build scripts/index.ts --compile --target=bun-darwin-x64 --outfile scripts/bin/safety-op-creds-darwin-x64",
    "build:windows": "bun build scripts/index.ts --compile --target=bun-windows-x64 --outfile scripts/bin/safety-op-creds-windows-x64.exe",
    "build:all": "bun run build:linux-x64 && bun run build:linux-arm64 && bun run build:darwin-arm64 && bun run build:darwin-x64 && bun run build:windows",
    "fetch-tools": "bun run build:all"
  },
  "files": [
    "SKILL.md",
    "scripts",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "claude-code",
    "skill",
    "hook",
    "secrets",
    "1password",
    "op",
    "credentials",
    "pretooluse"
  ]
}
